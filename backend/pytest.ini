[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: pruebas unitarias sin dependencias externas
    integration: pruebas sobre la API y base de datos (TestClient)
    postgres: requieren PostgreSQL real (opt-in, RUN_POSTGRES_TESTS=1)
